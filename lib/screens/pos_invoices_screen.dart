import 'package:flutter/material.dart';

class PosInvoicesScreen extends StatelessWidget {
  const PosInvoicesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Invoices Screen"));
  }
}
